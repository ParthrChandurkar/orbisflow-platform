package com.orbisflow.common.errors;

public enum ApiErrorCode {
    INVALID_REQUEST,
    AUTH_REQUIRED,
    INVALID_CREDENTIALS,
    ACCESS_DENIED,
    CSRF_INVALID,
    RESOURCE_NOT_FOUND,
    METHOD_NOT_ALLOWED,
    INTERNAL_ERROR
}
